# Copy to .env.local for development. Vite only exposes vars starting with VITE_
# No trailing slash. The axios client appends /api itself.
VITE_API_URL=http://localhost:5000

# In Vercel, set this to your Render backend URL instead:
# VITE_API_URL=https://your-service.onrender.com
